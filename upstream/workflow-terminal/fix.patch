diff -ruN '--exclude=Cargo.toml.orig' '--exclude=.cargo_vcs_info.json' '--exclude=.cargo-ok' /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/keys.rs extern/workflow-terminal/src/keys.rs
--- /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/keys.rs	2006-07-23 21:21:28.000000000 -0400
+++ extern/workflow-terminal/src/keys.rs	2026-09-05 12:54:59.561170066 -0300
@@ -14,6 +14,7 @@
     End,
     PageUp,
     PageDown,
+    Tab,
     BackTab,
     Delete,
     Insert,
diff -ruN '--exclude=Cargo.toml.orig' '--exclude=.cargo_vcs_info.json' '--exclude=.cargo-ok' /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/terminal/crossterm.rs extern/workflow-terminal/src/terminal/crossterm.rs
--- /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/terminal/crossterm.rs	2006-07-23 21:21:28.000000000 -0400
+++ extern/workflow-terminal/src/terminal/crossterm.rs	2026-09-05 12:54:59.561170066 -0300
@@ -61,8 +61,35 @@
         Ok(())
     }
 
+    /// Discard any keystrokes already buffered in the tty. Called before a
+    /// secret prompt opens: without this, a stale Enter (typed while output
+    /// was scrolling) is consumed as the entire password the instant the
+    /// prompt opens — the prompt flashes past, decrypt fails, and the user
+    /// retypes their password into the NORMAL editor (cleartext echo, stored
+    /// in Up-arrow history, executed as a command). Marigold fix; upstream
+    /// workflow-terminal 0.18.0 has no input flush at all.
+    pub fn flush_pending_input(&self) {
+        while let Ok(true) = event::poll(std::time::Duration::ZERO) {
+            if event::read().is_err() {
+                break;
+            }
+        }
+    }
+
     pub async fn intake(&self, terminate: &Arc<AtomicBool>) -> Result<()> {
         loop {
+            // Check terminate BEFORE blocking on input, polling so the check
+            // repeats: upstream checked only after a successful read, so a
+            // secondary intake loop spawned by `UserInput::capture` whose
+            // closing Enter was consumed elsewhere stayed blocked in
+            // `event::read()` forever — a ghost reader stealing keystrokes
+            // from every later prompt (Marigold fix).
+            if terminate.load(Ordering::SeqCst) {
+                break;
+            }
+            if !event::poll(std::time::Duration::from_millis(50))? {
+                continue;
+            }
             let event = event::read()?;
             // println!("{:?}",event);
             if let Event::Key(key) = event {
@@ -79,6 +106,7 @@
                             }
                         }
                         KeyCode::Enter => Key::Enter,
+                        KeyCode::Tab => Key::Tab,
                         KeyCode::Esc => Key::Esc,
                         KeyCode::Left => Key::ArrowLeft,
                         KeyCode::Right => Key::ArrowRight,
diff -ruN '--exclude=Cargo.toml.orig' '--exclude=.cargo_vcs_info.json' '--exclude=.cargo-ok' /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/terminal/mod.rs extern/workflow-terminal/src/terminal/mod.rs
--- /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/terminal/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ extern/workflow-terminal/src/terminal/mod.rs	2026-09-05 16:04:21.510986143 -0300
@@ -99,6 +99,11 @@
     secret: Arc<AtomicBool>,
     kbhit: Arc<AtomicBool>,
     terminate: Arc<AtomicBool>,
+    /// Set when the prompt is closed by Ctrl+C rather than Enter, so
+    /// `capture()` can surface cancellation as an error instead of returning
+    /// an empty string indistinguishable from a real empty answer
+    /// (Marigold fix).
+    aborted: Arc<AtomicBool>,
     sender: Sender<String>,
     receiver: Receiver<String>,
 }
@@ -113,6 +118,7 @@
             secret: Arc::new(AtomicBool::new(false)),
             kbhit: Arc::new(AtomicBool::new(false)),
             terminate: Arc::new(AtomicBool::new(false)),
+            aborted: Arc::new(AtomicBool::new(false)),
             sender,
             receiver,
         }
@@ -132,10 +138,18 @@
         self.secret.store(secret, Ordering::SeqCst);
         self.kbhit.store(kbhit, Ordering::SeqCst);
         self.terminate.store(false, Ordering::SeqCst);
+        self.aborted.store(false, Ordering::SeqCst);
         Ok(())
     }
 
     pub fn close(&self) -> Result<()> {
+        // Idempotent: a duplicate close (a ghost reader, or Ctrl+C racing
+        // Enter) must not send a second value — the upstream unconditional
+        // try_send().unwrap() panicked the runtime with "Channel TrySend
+        // Error: Full" once stray closes accumulated (Marigold fix).
+        if !self.enabled.swap(false, Ordering::SeqCst) {
+            return Ok(());
+        }
         let s = {
             self.prompt.lock().unwrap().take();
             let mut buffer = self.buffer.lock().unwrap();
@@ -144,9 +158,8 @@
             s
         };
 
-        self.enabled.store(false, Ordering::SeqCst);
         self.terminate.store(true, Ordering::SeqCst);
-        self.sender.try_send(s.to_string()).unwrap();
+        self.sender.try_send(s.to_string()).ok();
         Ok(())
     }
 
@@ -179,6 +192,9 @@
         }
 
         let string = self.receiver.recv().await?;
+        if self.aborted.swap(false, Ordering::SeqCst) {
+            return Err(Error::Custom("cancelled".to_string()));
+        }
         Ok(string)
     }
 
@@ -197,8 +213,14 @@
     fn ingest(&self, key: Key, term: &Arc<Terminal>) -> Result<()> {
         match key {
             Key::Ctrl('c') => {
+                // Cancel the PROMPT, not the application: upstream called
+                // term.abort() here, so changing your mind at a password
+                // prompt tore down the whole CLI (Marigold fix). The aborted
+                // flag turns capture()'s result into an error so callers can
+                // tell cancellation from an intentionally empty answer.
+                self.aborted.store(true, Ordering::SeqCst);
+                term.crlf();
                 self.close()?;
-                term.abort();
             }
             Key::Char(ch) => {
                 self.buffer.lock().unwrap().push(ch);
@@ -375,6 +397,10 @@
                     if !self.user_input.secret.load(Ordering::SeqCst) {
                         self.write(self.user_input.get_buffer());
                     }
+                } else {
+                    // kbhit-style prompt (no text): upstream dropped the
+                    // output line entirely here (Marigold fix).
+                    self.write(format!("{}{}\n\r", ClearLine, s.to_string()));
                 }
             } else {
                 self.write(format!("{}\n\r", s.to_string()));
@@ -384,7 +410,9 @@
             let data = self.inner().unwrap();
             let p = format!("{}{}", self.get_prompt(), data.buffer);
             self.write(p);
-            let l = data.buffer.len() - data.cursor;
+            // saturating: cursor/buffer are updated without a common lock and
+            // this repo builds with overflow-checks in release (Marigold fix).
+            let l = data.buffer.len().saturating_sub(data.cursor);
             for _ in 0..l {
                 self.write("\x08".to_string());
             }
@@ -395,12 +423,17 @@
     /// is useful when the prompt is handled externally and contains
     /// data that should be updated.
     pub fn refresh_prompt(&self) {
+        // Never repaint the normal `$ ` prompt over an open secret prompt
+        // (Marigold fix — upstream only checked is_running).
+        if self.user_input.is_enabled() {
+            return;
+        }
         if !self.is_running() {
             self.write(format!("{}", ClearLine));
             let data = self.inner().unwrap();
             let p = format!("{}{}", self.get_prompt(), data.buffer);
             self.write(p);
-            let l = data.buffer.len() - data.cursor;
+            let l = data.buffer.len().saturating_sub(data.cursor);
             for _ in 0..l {
                 self.write("\x08".to_string());
             }
@@ -548,10 +581,18 @@
     /// user input (useful for password entry)
     pub async fn ask(self: &Arc<Terminal>, secret: bool, prompt: &str) -> Result<String> {
         self.reset_line_buffer();
+        // Drop any type-ahead BEFORE the modal prompt opens, and again after
+        // it closes — a buffered stray Enter must never become the answer to
+        // a password prompt, and keys typed a beat too late must never leak
+        // into the next command line (Marigold fix, see crossterm.rs).
+        self.term().flush_pending_input();
         self.term().write(prompt.to_string());
-        self.user_input
+        let answer = self
+            .user_input
             .capture(secret, false, Some(prompt.to_string()), self)
-            .await
+            .await;
+        self.term().flush_pending_input();
+        answer
     }
 
     pub async fn kbhit(self: &Arc<Terminal>, prompt: Option<&str>) -> Result<String> {
@@ -607,6 +648,16 @@
             return Ok(());
         }
 
+        // While a command is executing, the only meaningful keystroke is
+        // Ctrl+C. Everything else is discarded: upstream fed it into the
+        // normal line editor, so characters typed toward a prompt that had
+        // already closed (or not yet opened) were echoed, recorded in
+        // history, and executed as commands — the exact path by which a
+        // wallet password ended up in cleartext scrollback (Marigold fix).
+        if self.is_running() && !matches!(key, Key::Ctrl('c')) {
+            return Ok(());
+        }
+
         match key {
             Key::Backspace => {
                 let mut data = self.inner()?;
@@ -619,6 +670,59 @@
                 data.buffer.remove(idx);
                 self.trail(data.cursor, &data.buffer, true, true, 0);
             }
+            Key::Tab => {
+                // Tab completion (Marigold fix — upstream never mapped Tab and
+                // the Cli::complete hook was dead). Contract with the handler:
+                // `complete(line)` returns full-line candidates. One candidate
+                // completes in place; several extend to their common prefix or
+                // list the alternatives (last token only, for brevity).
+                let buffer = {
+                    let data = self.inner()?;
+                    if data.cursor != data.buffer.len() {
+                        return Ok(());
+                    }
+                    data.buffer.clone().to_string()
+                };
+                if buffer.trim().is_empty() {
+                    return Ok(());
+                }
+                if let Ok(Some(candidates)) = self.handler.clone().complete(self.clone(), buffer.clone()).await {
+                    match candidates.len() {
+                        0 => {}
+                        1 => {
+                            let mut data = self.inner()?;
+                            data.buffer = format!("{} ", candidates[0]).into();
+                            data.cursor = data.buffer.len();
+                            let p = format!("{}{}{}", ClearLine, self.get_prompt(), data.buffer);
+                            self.write(p);
+                        }
+                        _ => {
+                            let mut prefix = candidates[0].clone();
+                            for candidate in candidates.iter().skip(1) {
+                                let common = prefix.chars().zip(candidate.chars()).take_while(|(a, b)| a == b).count();
+                                prefix.truncate(prefix.char_indices().nth(common).map(|(i, _)| i).unwrap_or(prefix.len()));
+                            }
+                            if prefix.len() > buffer.len() {
+                                let mut data = self.inner()?;
+                                data.buffer = prefix.into();
+                                data.cursor = data.buffer.len();
+                                let p = format!("{}{}{}", ClearLine, self.get_prompt(), data.buffer);
+                                self.write(p);
+                            } else {
+                                let display = candidates
+                                    .iter()
+                                    .map(|c| c.rsplit(' ').next().unwrap_or(c.as_str()).to_string())
+                                    .collect::<Vec<_>>()
+                                    .join("  ");
+                                self.write(format!("{}\n\r{}\n\r", ClearLine, display));
+                                let data = self.inner()?;
+                                let p = format!("{}{}", self.get_prompt(), data.buffer);
+                                self.write(p);
+                            }
+                        }
+                    }
+                }
+            }
             Key::ArrowUp => {
                 let mut data = self.inner()?;
                 if data.history_index == 0 {
@@ -716,10 +820,33 @@
                 return Ok(());
             }
             Key::Ctrl('c') => {
-                cfg_if! {
-                    if #[cfg(not(target_arch = "wasm32"))] {
-                        self.exit().await;
+                // Shell semantics (Marigold fix): at an idle prompt Ctrl+C
+                // clears the line and redraws — only `exit` exits. While a
+                // command is RUNNING it keeps upstream's app-exit behavior,
+                // as the escape hatch from a hung command.
+                if self.is_running() {
+                    cfg_if! {
+                        if #[cfg(not(target_arch = "wasm32"))] {
+                            self.exit().await;
+                        }
                     }
+                } else {
+                    let mut data = self.inner()?;
+                    data.buffer.clear();
+                    data.cursor = 0;
+                    self.write(format!("^C  (type 'exit' or press Ctrl+D to quit)\n\r{}", self.get_prompt()));
+                }
+                return Ok(());
+            }
+            Key::Ctrl('d') => {
+                // EOF at an empty line exits, like every modern REPL
+                // (Marigold fix). With text on the line it does nothing.
+                let empty = { self.inner()?.buffer.is_empty() };
+                if empty {
+                    self.crlf();
+                    self.running.store(true, Ordering::SeqCst);
+                    self.exec("exit".to_string()).await.ok();
+                    self.running.store(false, Ordering::SeqCst);
                 }
                 return Ok(());
             }
diff -ruN '--exclude=Cargo.toml.orig' '--exclude=.cargo_vcs_info.json' '--exclude=.cargo-ok' /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/terminal/termion.rs extern/workflow-terminal/src/terminal/termion.rs
--- /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/terminal/termion.rs	2006-07-23 21:21:28.000000000 -0400
+++ extern/workflow-terminal/src/terminal/termion.rs	2026-09-04 21:43:34.681748899 -0300
@@ -21,6 +21,8 @@
 }
 
 impl Termion {
+    /// No-op on this backend; see crossterm.rs (Marigold fix).
+    pub fn flush_pending_input(&self) {}
     pub fn try_new() -> Result<Self> {
         Self::try_new_with_options(&Options::default())
     }
diff -ruN '--exclude=Cargo.toml.orig' '--exclude=.cargo_vcs_info.json' '--exclude=.cargo-ok' /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/terminal/xterm.rs extern/workflow-terminal/src/terminal/xterm.rs
--- /home/dom/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/workflow-terminal-0.18.0/src/terminal/xterm.rs	2006-07-23 21:21:28.000000000 -0400
+++ extern/workflow-terminal/src/terminal/xterm.rs	2026-09-04 21:43:34.681748899 -0300
@@ -182,6 +182,8 @@
 unsafe impl Sync for Xterm {}
 
 impl Xterm {
+    /// No-op on this backend; see crossterm.rs (Marigold fix).
+    pub fn flush_pending_input(&self) {}
     pub fn try_new() -> Result<Self> {
         Self::try_new_with_options(&Options::default())
     }
